import { ToggleHighlightDirective } from './toggle-highlight.directive';

describe('ToggleHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new ToggleHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
